"""Provide database connections, schema setup, settings and filesystem locks.

``runtime`` adapts a project's engine provider; ``offline_sqlite`` supplies
local audit storage. Model and publication decisions belong to their callers.
"""
